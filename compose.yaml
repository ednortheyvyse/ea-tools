services:
  eatools:
    image: eatools
    build:
      context: .
      dockerfile: ./Dockerfile
    command: ["npm", "run", "dev"]
    environment:
      NODE_ENV: development
    ports:
      - 3000:3000
    depends_on:
      - backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
      - "5678:5678"
    volumes:
      - ./backend:/app
    command: ["python", "-m", "debugpy", "--listen", "0.0.0.0:5678", "main.py"]
